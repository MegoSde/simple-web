# Copy to .env and adjust as needed
POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=cms
POSTGRES_PORT=5432

# Optional: change volume host paths if needed
PGDATA_HOST=./docker/db/data
PGINIT_HOST=./docker/db/init
