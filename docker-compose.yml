services:
  db:
    image: postgres:16-alpine
    container_name: simple-web_db
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 10
    volumes:
      - ${PGDATA_HOST:-./container/db/data}:/var/lib/postgresql/data
    deploy:
      resources:
        limits:
          memory: 512M
          
  flyway:
    image: flyway/flyway:10-alpine
    depends_on:
      db:
        condition: service_healthy
    command: >
      -url=jdbc:postgresql://db:${POSTGRES_PORT:-5432}/${POSTGRES_DB}
      -user=${POSTGRES_USER}
      -password=${POSTGRES_PASSWORD}
      -connectRetries=60
      migrate
    volumes:
      - ./container/flyway/sql:/flyway/sql:ro
    restart: "no"
  
  
  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z-cpuv1
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:?set-in-.env}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:?set-in-.env}
    volumes:
      - ./container/minio/data:/data
    ports:
      - "9000:9000"   # S3 API (internt brug)
      - "9001:9001"   # MinIO Console (kun internt!)
    healthcheck:
      test: ["CMD", "curl", "-f", "http://minio:9000/minio/health/live"]
      interval: 10s
      timeout: 2s
      retries: 10

  minio-init:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z-cpuv1
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      MINIO_ENDPOINT: ${MINIO_ENDPOINT-http://minio:9000}
      MINIO_BUCKETS: ${MINIO_BUCKETS-originals,work,thumbnail}
      MINIO_ANON_READ_BUCKETS: ${MINIO_ANON_READ_BUCKETS-work,thumbnail}
      APP_MEDIA_ACCESS_KEY: ${APP_MEDIA_ACCESS_KEY-}
      APP_MEDIA_SECRET_KEY: ${APP_MEDIA_SECRET_KEY-}
      APP_MEDIA_POLICY: ${APP_MEDIA_POLICY-app-media-policy}
    volumes:
      - ./container/minio/init/minio-init.sh:/init/minio-init.sh:ro
      - ./container/minio/logs:/logs
    entrypoint: ["/bin/sh","-lc","tr -d '\r' < /init/minio-init.sh | /bin/sh"]